@model KernalTravelGuide.Models.TouristSpot

@{
    ViewData["Title"] = "Delete Tourist Spot";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Delete Tourist Spot</h1>

<h3>Are you sure you want to delete this tourist spot?</h3>

<div class="row">

    <div class="col-md-4">

        @if (!string.IsNullOrEmpty(Model.ImagePath))
        {
            <img src="@Model.ImagePath"
                 alt="@Model.Name"
                 style="width:100%;max-width:300px;height:200px;object-fit:cover;border-radius:8px;" />
        }

    </div>

    <div class="col-md-8">

        <dl class="row">

            <dt class="col-sm-4">
                Name
            </dt>

            <dd class="col-sm-8">
                @Model.Name
            </dd>

            <dt class="col-sm-4">
                City
            </dt>

            <dd class="col-sm-8">
                @Model.City?.Name
            </dd>

            <dt class="col-sm-4">
                Entry Fee
            </dt>

            <dd class="col-sm-8">
                @Model.EntryFee
            </dd>

            <dt class="col-sm-4">
                Status
            </dt>

            <dd class="col-sm-8">
                @(Model.IsActive ? "Active" : "Inactive")
            </dd>

        </dl>

        <form asp-action="Delete"
              method="post">

            <input type="hidden"
                   asp-for="Id" />

            <button type="submit"
                    class="btn btn-danger">
                Delete
            </button>

            <a asp-action="Index"
               class="btn btn-secondary">
                Cancel
            </a>

        </form>

    </div>

</div>